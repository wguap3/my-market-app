DROP TABLEIF EXISTS items CASCADE;

CREATE TABLE items (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    imgPath VARCHAR(255),
    price DECIMAL(10, 2) NOT NULL,
    count INT NOT NULL
);

